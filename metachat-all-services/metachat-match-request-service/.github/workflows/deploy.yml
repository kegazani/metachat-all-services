name: Deploy Match Request Service
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SERVICES_SSH_KEY }}
      - name: Build and deploy
        env:
          SERVICES_HOST: ${{ secrets.SERVICES_HOST }}
          SERVICES_USER: ${{ secrets.SERVICES_USER }}
          INFRA_POSTGRES: ${{ secrets.INFRA_POSTGRES_HOST }}
          POSTGRES_USER: ${{ secrets.INFRA_POSTGRES_USER }}
          POSTGRES_PASSWORD: ${{ secrets.INFRA_POSTGRES_PASSWORD }}
          POSTGRES_DB: ${{ secrets.INFRA_POSTGRES_DB }}
        run: |
          ssh -o StrictHostKeyChecking=no $SERVICES_USER@$SERVICES_HOST "mkdir -p /opt/metachat-services/match-request-service"
          rsync -az --delete -e "ssh -o StrictHostKeyChecking=no" ./ $SERVICES_USER@$SERVICES_HOST:/opt/metachat-services/match-request-service
          ssh -o StrictHostKeyChecking=no $SERVICES_USER@$SERVICES_HOST "cd /opt/metachat-services/match-request-service && docker build -t metachat/match-request-service:latest . && docker service update --image metachat/match-request-service:latest --env-add DATABASE_HOST=$INFRA_POSTGRES --env-add DATABASE_USER=$POSTGRES_USER --env-add DATABASE_PASSWORD=$POSTGRES_PASSWORD --env-add DATABASE_NAME=$POSTGRES_DB --env-add DATABASE_PORT=5432 --env-add SERVER_PORT=50054 --publish-add 50054:50054 metachat-services_match-request-service || docker service create --name metachat-services_match-request-service --network metachat_overlay -e DATABASE_HOST=$INFRA_POSTGRES -e DATABASE_PORT=5432 -e DATABASE_USER=$POSTGRES_USER -e DATABASE_PASSWORD=$POSTGRES_PASSWORD -e DATABASE_NAME=$POSTGRES_DB -e SERVER_PORT=50054 -p 50054:50054 metachat/match-request-service:latest"

